name: CI on Commit

on:
  push:       # 🔹 Se ejecuta en cada commit enviado con git push
    branches:
      - '**'  # 🔹 En todas las ramas (usa "main" o "develop" si quieres limitarlo)

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Validar estructura del proyecto
        run: |
          echo "Commit detectado 🚀"
          echo "Archivos en el repo:"
          ls -R
